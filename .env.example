# Copy to .env and fill in. Never commit .env.
# Every key maps to src/common/settings.py (case-insensitive).
# Unset keys fall back to the defaults shown in comments.

# MinIO / S3 (docker-compose.yml also uses MINIO_ROOT_USER/PASSWORD)
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=change-me
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=

# Nessie catalog
NESSIE_HOST=localhost
NESSIE_PORT=19120
NESSIE_REF=main
NESSIE_API_VERSION=v2

# Redpanda / Kafka
REDPANDA_HOST=localhost
KAFKA_BROKER=localhost:19092
SCHEMA_REGISTRY_URL=http://localhost:8081
TOPIC_NAME=gateway_webhooks

# Spark — local | cluster (spark://) | yarn (Hadoop, see docker-compose.hadoop.yml)
SPARK_MODE=local
SPARK_MASTER=local[*]
SPARK_DRIVER_MEMORY=2g
SPARK_EXECUTOR_MEMORY=2g
SPARK_EXECUTOR_CORES=2
SPARK_SHUFFLE_PARTITIONS=200
# Jar packages default to the full Iceberg/Kafka/Nessie/S3 list in settings.py.
# Leave commented unless overriding; an empty value DISABLES all jars.
# SPARK_JAR_PACKAGES=
LOAD_CSV_ON_DRIVER=false
SPARK_YARN_DEPLOY_MODE=client

# Iceberg warehouses + YARN
ICEBERG_WAREHOUSE=s3a://lakehouse/warehouse
ICEBERG_WAREHOUSE_HDFS=hdfs://namenode:8020/warehouse
HADOOP_CONF_DIR=
YARN_RESOURCEMANAGER_ADDRESS=resourcemanager:8032
SPARK_YARN_STAGING_DIR=hdfs://namenode:8020/tmp/spark-staging

# Tables (TABLE_PREFIX=glue retargets nessie.db.* to glue.db.* for AWS)
WEBHOOK_TABLE=nessie.db.webhooks
WEBHOOK_TABLE_HDFS=nessie_hdfs.db.webhooks
DLQ_TABLE=nessie.db.webhooks_dlq
DLQ_TABLE_HDFS=nessie_hdfs.db.webhooks_dlq
TABLE_PREFIX=

# Fee engine
FEE_RATE_CONFIG=config/fee_rates.yaml
DEFAULT_MDR_RATE=0.015

# Project
PROJECT_ROOT=.

# Bench tuning (empty = production defaults)
BENCH_MODE=
BENCH_SHUFFLE=
BENCH_WAREHOUSE=

# Set automatically by their environments; only override to debug
# AIRFLOW_HOME=
# JAVA_HOME=
# SPARK_HOME=
