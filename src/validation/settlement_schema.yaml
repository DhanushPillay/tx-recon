schema_type: dataframe
api: pandas
columns:
  transaction_id:
    dtype: string[python]
    unique: true
  settled_amount_paise:
    dtype: int64
    greater_than: 0
  bank_ref_id:
    dtype: string[python]
